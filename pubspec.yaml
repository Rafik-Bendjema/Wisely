name: wisely
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  animated_text_kit: 
  flutter_hooks: 
  hooks_riverpod: 
  flutter_svg:
  persistent_bottom_nav_bar:
  hive: 
  hive_flutter: 
  path_provider: 
  uuid:
  intl: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: 
  build_runner: 
  

flutter:
  uses-material-design: true
  assets:
    - assets/images/
